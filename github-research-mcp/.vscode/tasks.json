{
    "tasks": [
        {
            "label": "Create Inline Snapshots",
            "type": "shell",
            "command": "uv run pytest --inline-snapshot=create"
        },
        {
            "label": "Update Inline Snapshots",
            "type": "shell",
            "command": "uv run pytest --inline-snapshot=fix"
        }
    ]
}